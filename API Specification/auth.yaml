/auth/register:
  post:
    tags: [Auth]
    summary: Đăng ký tài khoản khách hàng (FR01)
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [fullName, phone, password]
            properties:
              fullName: { type: string, example: "Nguyen Van A" }
              phone: { type: string, example: "0901234567" }
              password: { type: string, format: password }
    responses:
      "201":
        description: Tạo tài khoản thành công
      "400":
        description: Dữ liệu không hợp lệ (AC01)

/auth/login:
  post:
    tags: [Auth]
    summary: Đăng nhập (FR02, AC29)
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [phone, password]
            properties:
              phone: { type: string }
              password: { type: string, format: password }
    responses:
      "200":
        description: Đăng nhập thành công, trả về access token
        content:
          application/json:
            schema:
              type: object
              properties:
                accessToken: { type: string }
                role: { type: string, enum: [customer, driver, operator] }
      "401":
        description: Sai thông tin đăng nhập (AC02)
